services:
  - type: web
    runtime: static
    name: todo
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist/todo/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
